Place your GLB map under this folder.

Recommended path:

  assets/maps/map.glb

The code loads the default scene by path:

  maps/map.glb#Scene0

If your GLB has a different name or scene index, update `MAP_SCENE_PATH` in `src/main.rs` accordingly.
